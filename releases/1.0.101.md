# Kenji Space 1.0.101

Отдельный релиз. Тег `v1.0.58` не трогаем.

30–40с без окон: `FastScan` + `FirmwareHunt` (до 500 папок на каждый корень) + `DataSeed.ensure` + `recreate()` шли на том же диске, что и NSP.

Фон — микропотоки `THREAD_PRIORITY_LOWEST`, параллельно. На запуске игры всё встаёт (`IdleWork.pause`). Охота прошивки урезана. Авто-`recreate` полки с сида убран.
