# Kenji Space 1.0.102

Отдельный релиз. Тег `v1.0.58` не трогаем.

Игровой слой Kenji перекрывал наши вьюхи. Чип ⏳, FPS и плавающая ⚙ (пауза, пресеты) вешаются в **тот же** ViewGroup, что и игра, `bringToFront`. Полка только если виден Search. Extra-окна игры не гасим.
