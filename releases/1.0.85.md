# Kenji Space 1.0.85

Отдельный релиз. Тег `v1.0.58` не трогаем.

Основа — **1.0.75** (их полка, обложки, запуск, панель Space, живой индикатор).

Починка индикации:

- их серый Loading делается невидимым (`alpha = 0`, `INVISIBLE`, не ловит тачи);
- остаётся **один** наш индикатор загрузки;
- опрос во время загрузки чаще (250 мс), чтобы их окно не успевало перекрыть.
