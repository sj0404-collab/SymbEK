# Kenji Space 1.0.95

Отдельный релиз. Тег `v1.0.58` не трогаем.

Их Loading / проверка шейдеров — **полноэкранное** окно с `FLAG_SECURE`. Мы его пропускали (думали «это игра»), поэтому оно накрывало всё снаружи, в том числе скрины.

Теперь это окно: alpha 0, без dim, без SECURE, не перехватывает тачи. Шейдеры собираются дальше. Наши ⏳ — замена. Decor с Surface не трогаем.
