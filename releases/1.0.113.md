# Kenji Space 1.0.113

Обложки вернулись. Не читаем весь NSP (это OOM) и не зовём `deviceGetGameInfo`.

Берём JPEG из PFS0 (маленькие NCA / control) кусками, либо файл рядом с игрой, либо кэш `space_icons/`. Один раз — дальше с диска.
