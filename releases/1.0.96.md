# Kenji Space 1.0.96

Отдельный релиз. Тег `v1.0.58` не трогаем.

1.0.95 обнулял fullscreen-окно Kenji. Игра у них **Skia/Compose**, не SurfaceView — звук шёл, картинка была белой, видны только наши ⏳.

Больше не трогаем слой с игрой. `EmulationService` = игра идёт. SECURE снимаем. Маленький Loading-попап можно прятать, полный экран — нет.
