# Kenji Space 1.0.91

Отдельный релиз. Тег `v1.0.58` не трогаем.

Официальный Kenji читает полку как дерево SAF (`content://…/tree/…`) и `gameFolderPath`. Сканер писал обычный `/storage/…` — `getGameList` пустой, обложки падали в их `NotAvailableIcon`.

Теперь: URI + путь, `HomeViewModel.reloadGameList()`, после выдачи «всех файлов» сканер сам перечитывает полку.
