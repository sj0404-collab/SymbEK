# Kenji Space 1.0.82

Отдельный релиз. Тег `v1.0.58` не трогаем.

Вкладка «Настройки» больше не роняет приложение: строка папки отдавала вид, у которого уже был родитель (`IllegalStateException`). Экран настроек обёрнут, ошибка рисуется текстом, а не крашем.
