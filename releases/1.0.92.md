# Kenji Space 1.0.92

Отдельный релиз. Тег `v1.0.58` не трогаем.

Полка пустая при 2 ROM в `Download/ed`: 1.0.91 после «+» выбрасывал выданный SAF URI и писал синтетический `content://…/tree/…` без persistable-права. Kenji `fromTreeUri` видел пустое дерево — отсюда пустая сетка и их `NotAvailableIcon`. Stash / `registered/00` ни при чём: 234 NCA на месте.

Чиним: храним тот URI, который выдал системный выбор папки. Фейковый URI с 1.0.91 снимаем. Без SAF — обычный путь (all-files / `fromFullPath`).
