# Kenji Space 1.0.93

Отдельный релиз. Тег `v1.0.58` не трогаем.

На полке висели официальные «Loading» и песочные часы Kenji, при этом GameHost не открывался. Карточка Compose в том же окне, что и сетка — мы её пропускали. `reloadGameList` сам поднимал этот оверлей.

Прячем карточку и прогресс (без затемнения). `reloadGameList` больше не зовём. Часы с рикошетом только если крутится `EmulationService`.
