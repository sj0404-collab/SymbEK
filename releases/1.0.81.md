# Kenji Space 1.0.81

Отдельный релиз. Тег `v1.0.58` не трогаем.

Починка лаунчера 1.0.80 по мотивам Symbiosis Eden:

- **Запустить** шлёт только `bootPath` / `titleName` / `titleId` — без `file://`, из‑за него на Android 7+ вылетало.
- Список не перечитывается на каждом resume, только после смены папки.
- Дефолтные Download/ed больше сами не подставляются.
- Обложки подгружаются без перерисовки всего экрана.
- После запуска — карманный секундомер как в Eden (таскается, исчезает когда в игре есть звук).
- В игре только ⚙ пресетов. Официальный Kenji играет как в 1.0.25.
