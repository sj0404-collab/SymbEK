# Kenji Space 1.0.94

Отдельный релиз. Тег `v1.0.58` не трогаем.

Два лоадера и вечный 60fps-рикошет сажали загрузку (11с → 40с), а `INVISIBLE` по дереву Kenji ломал UI и скриншоты до выхода из игры.

- Их Loading прячем (только попап, без порчи сетки).
- Наши часы в idle (⏳, пульс раз в 0.5с). Тап — скрыть. Долгое — выкл. Дёрнуть — рикошет как антистресс.
- В игре индикатор не крутится и окна Kenji не трогаем. FLAG_SECURE снимаем.
