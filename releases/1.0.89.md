# Kenji Space 1.0.89

Отдельный релиз. Тег `v1.0.58` не трогаем.

- Сканер больше не молчит: без доступа ко всем файлам просит его, после поиска перечитывает полку Kenji (`gameFolder` писался слишком поздно — сетка оставалась пустой).
- Полоска загрузки не встаёт в настройках Kenji. Тап по строке настроек больше не считается тапом по обложке.
- Пустой тап по полке не держит индикатор. Он только если Kenji реально грузит игру.
