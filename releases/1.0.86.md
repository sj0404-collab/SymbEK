# Kenji Space 1.0.86

Отдельный релиз. Тег `v1.0.58` не трогаем.

Основа **1.0.75**, логика индикатора как в **1.0.60** (там уже почти всё играло).

Критические баги:

- второе окно LoadOverlay больше не создаётся — оно и наезжало на их Loading;
- полоска загрузки снова **внутри** активности (процент и шаг из журнала, как в 0.60);
- их серый Loading только прозрачный (`alpha = 0`), не GONE;
- в чужие окна ничего не втыкаем — из‑за этого закрывались другие приложения;
- потеря фокуса окна = идёт загрузка (как в 0.60), индикатор не снимается через 8 секунд.
