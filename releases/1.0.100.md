# Kenji Space 1.0.100

Отдельный релиз. Тег `v1.0.58` не трогаем.

Их спиннер — модалка на весь экран, привязана к плееру: блокирует тачи, скрины и UI ~40с, потом сама гаснет. Наш ⏳ — плавающая кнопка.

Пока игра бутится, extra-окно лоадера (не content с игрой/геймпадом) прячем раз в секунду: alpha 0, без SECURE, без перехвата тачей. Content не трогаем — белого экрана нет.
