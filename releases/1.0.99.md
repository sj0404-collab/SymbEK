# Kenji Space 1.0.99

Отдельный релиз. Тег `v1.0.58` не трогаем.

Запуск 40с/30с вместо ~10с: во время бута мы каждые 300мс обходили все окна Kenji. Это не их лоадер, это мы.

- Во время запуска не сканируем окна, не трогаем content, `ensure` выключен.
- Их Loading (окно с текстом Loading) прячем **один раз**.
- Часы idle сверху, как просили.
- WebView/сайт не делаем: HTML уже ломал обложки и запуск. Играет официальный GameHost.
