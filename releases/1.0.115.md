# Kenji Space 1.0.115

Обложки: не читаем весь NSP и не зовём `deviceGetGameInfo`.

Ищем по порядку: файл рядом с игрой → кэш Eden/Yuzu/Kenji (`cache/game_list/{id}.png`) → то, что Kenji уже нарисовал → native *Icon* → мелкий PFS0/JPEG.

Кэш в `space_icons/`. Обновление по-прежнему «+», не обложка игры.
