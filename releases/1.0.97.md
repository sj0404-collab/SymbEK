# Kenji Space 1.0.97

Отдельный релиз. Тег `v1.0.58` не трогаем.

Слои: игра + геймпад живут в `content` — не трогаем. Их Loading / шейдеры — отдельное окно поверх. 1.0.95 гасил content (белый экран). 1.0.96 возвращал extra-окна (их лоадер снова сверху, игра и геймпад пропали).

Прячем только extra-окно Loading. Content не обнуляем и не «восстанавливаем» чужие окна. PlayHud пропускает тачи мимо своих кнопок на геймпад.
